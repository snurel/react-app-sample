language: generic
sudo: required
services: 
    - docker
before_install:
    - docker build -t 1154455/react-sample-app -f Dockerfile.dev .
script:
    - docker run 1154455/react-sample-app npm run test -- --coverage